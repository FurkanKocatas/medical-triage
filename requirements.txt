# Türkçe Tıbbi Triaj — Bağımlılıklar
# Python 3.12 önerilir. Eğitim için CUDA 12.x destekli bir GPU gereklidir.
#
# PyTorch'u platformunuza uygun CUDA wheel'i ile AYRI kurun, örn:
#   pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/cu128
# (Proje NVIDIA GB10/Blackwell ve RTX 4070 üzerinde torch 2.11.0 + CUDA 12.8 ile çalıştırıldı.)
torch==2.11.0

# --- Çekirdek ML / fine-tuning yığını (kullanılan sürümler) ---
transformers==5.6.2
peft==0.19.1
trl==1.2.0
datasets==4.8.4
accelerate==1.13.0
bitsandbytes==0.49.2          # 4-bit NF4 QLoRA için (CUDA gerekir)
safetensors

# --- Veri işleme / metrikler ---
numpy==2.4.0
pandas==3.0.0
scikit-learn==1.8.0
evaluate

# --- Tokenizer yardımcıları (SentencePiece tabanlı modeller için) ---
sentencepiece
protobuf

# --- Görselleştirme (eval/plots) ---
matplotlib

# --- Hugging Face Hub (model/veri indirme, opsiyonel yükleme) ---
huggingface_hub
